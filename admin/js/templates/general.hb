<form>
  <fieldset>
    <legend>Webpage</legend>
    <label for="title">title:</label><input type="text" name="title" value="{{data.title}}"/><br />
    <label for="subtitle">subtitle:</label><input type="text" name="subtitle" value="{{data.subtitle}}" /><br />
    <label for="subtitle">location:</label><input type="text" name="location" value="{{data.location}}" /><br />
    <label for="subtitle">latitude:</label><input type="text" name="latitude" value="{{data.latitude}}" /><br />
    <label for="subtitle">longitude:</label><input type="text" name="longitude" value="{{data.longitude}}" /><br />
    <button type="button" id="btnGeneralSubmit">Save</button>
  </fieldset>
</form> 